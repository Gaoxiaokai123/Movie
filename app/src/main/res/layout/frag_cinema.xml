<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="#141931">

    <include layout="@layout/head"></include>

    <android.support.design.widget.TabLayout
        android:id="@+id/frag_cinema_tab"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:background="#1c2243"
        app:tabTextColor="@android:color/white"
        android:layout_weight="1"></android.support.design.widget.TabLayout>

    <!--<android.support.design.widget.TabLayout            -->
    <!--android:id="@+id/tablayout_video"            -->
    <!--android:layout_width="match_parent"            -->
    <!--android:layout_height="@dimen/tablayout_height"            -->
    <!--app:tabBackground="@drawable/tablayout_background"            -->
    <!--app:tabIndicatorHeight="0dp"            -->
    <!--app:tabSelectedTextColor="@color/white"            -->
    <!--app:tabTextAppearance="@style/TabLayoutTextStyle"            -->
    <!--app:tabTextColor="@color/white" />-->

    <android.support.v4.view.ViewPager
        android:id="@+id/frag_cinema_pager"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="13"></android.support.v4.view.ViewPager>

</LinearLayout>